import { Home, ListTodo, Calendar, Settings, ShoppingCart, DollarSign } from 'lucide-react-native';

const HomeIcon = <Home />;
const TasksIcon = <ListTodo />;
const CalendarIcon = <Calendar />;
const SettingsIcon = <Settings />;
const ShopIcon = <ShoppingCart />;
const FinancesIcon = <DollarSign />;

export { HomeIcon, TasksIcon, CalendarIcon, SettingsIcon, ShopIcon, FinancesIcon };
